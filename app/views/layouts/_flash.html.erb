<% flash.each do |key, value| %>
  	<div class="alert alert-<%= bootstrap_class_for(key) %> w-50">
  		<button type="button" class="close" data-dismiss="alert">x
  		</button>
    	<%= value %>
  	</div>
<% end %>